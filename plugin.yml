name: PerWorldPlayer
main: BlockHorizons\PerWorldPlayer\Loader
api: 4.0.1
version: 0.4.3

permissions:
  per-world-player.bypass:
    default: false
    description: Allows bypassing the per world player.